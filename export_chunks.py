import subprocess
import os
import json

def get_chunk(table, offset, limit=50):
    # This is a bit recursive because I can't call the tool from here.
    # I'll generate a list of commands.
    pass
